name: My First Workflow
on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome message
              run:  cat hello.sh #echo "My first GitHub Actions Job"
            - name: list files
              run: ls